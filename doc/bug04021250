现在我对所有文件有了完整的理解，来做一次系统性的 code review，逐一定位 bug。现在给你一个完整的代码审查报告，发现了 **7 个 Bug**，按严重程度分类：---

下面逐条解释每个 Bug 的根因和最小修复方案：

**Bug 1（致命）— 下坡 grade 永远 ≥ 0**

`_create_segments()` 里用 `seg_gain`（只累加上升量）计算 `avg_grade`，导致所有下坡段的坡度被计算为 0，模型完全无法区分平路和下坡。

```python
# 错误写法
avg_grade = (seg_gain / seg_dist_km / 10) if seg_dist_km > 0 else 0

# 正确写法：用净高差（可正可负）
net_ele = current_points[-1]['ele'] - current_points[0]['ele']
avg_grade = (net_ele / (seg_dist_km * 1000)) * 100
```

**Bug 2（致命）— `analyze_and_train()` 方法不存在**

`main()` 调用 `predictor.analyze_and_train()`，但 `MLRacePredictor` 只定义了 `train_from_files()`，运行时直接 `AttributeError`。最简单的修复是加一个别名：

```python
def analyze_and_train(self):
    # 找到 records_dir 下的文件，调用 train_from_files
    files = list(Path(self.records_dir).rglob('*.fit')) + \
            list(Path(self.records_dir).rglob('*.json'))
    return self.train_from_files([str(f) for f in files])
```

注意 `MLRacePredictor.__init__` 也没有接受 `records_dir` 参数，但 `main()` 里传入了，这个也需要同步修复。

**Bug 3（显著）— 速度用最后一步的瞬时速度而非段平均速度**

特征提取器里计算速度只用了当前 GPS 点和上一个点的时间差，而该"段"跨越了多个 GPS 点。需要跟踪分段累计时间：

```python
current_seg_time = 0   # 与 current_seg_distance 同步维护

# 在循环里
time_diff = timestamps[i] - timestamps[i-1]
current_seg_time += time_diff

# 分段时
speed = (current_seg_distance / 1000) / (current_seg_time / 3600) if current_seg_time > 0 else 5
current_seg_time = 0   # 重置
```

**Bug 4（显著）— 特征提取中 `avg_grade` = 最后一个点的瞬时坡度**

```python
# 错误：只取最后一个 GPS 点的坡度
avg_grade = seg_grade

# 正确：记录段开始索引，对段内所有点求均值
avg_grade = np.mean(grades[seg_start_idx : i + 1])
```

**Bug 5（显著）— GPX 段的 `rolling_grade_500m` 直接复制 `avg_grade`**

对于 FIT 数据的特征提取，代码确实用了滑动窗口计算滚动坡度（第 448 行），但 GPX 的 `_create_segments()` 第 831–832 行直接 `rolling_grade_500m=avg_grade`，完全没有滑动窗口。这意味着 LightGBM 的两个坡度特征对 GPX 段来说是完全相同的值，浪费了一个关键特征。

**Bug 6（轻微）— 滤波后 GPX 保存时丢弃超出原始点数的重采样点**

`save_filtered_gpx()` 第 331 行的 `if i < len(raw_points)` 使得当重采样点数多于原始点数时（20 m 重采样后几乎必然发生），循环提前退出。去掉这个条件，改用 `raw_idx = np.argmin(...)` 索引即可。

**Bug 7（轻微）— FIT 坡度计算未过滤 GPS 漂移**

两处内联坡度计算用 `if dist_m > 0` 而非 `if dist_m > 0.5`，当 GPS 静止漂移产生 0.1 m 的虚假位移时，会产生极端坡度。这与 `ElevationFilter.calculate_grade()` 里的 `min_distance_m` 设计不一致。